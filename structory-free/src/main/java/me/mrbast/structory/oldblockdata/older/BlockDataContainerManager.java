package me.mrbast.structory.oldblockdata.older;

import org.bukkit.block.Block;

import java.util.HashMap;
import java.util.Map;

public class BlockDataContainerManager {


    /*
    private static BlockDataContainerManager instance;
    public static BlockDataContainerManager getInstance() {
        if (instance == null) {
            instance = new BlockDataContainerManager();
        }
        return instance;
    }

     */

    private final Map<Block, FileDataContainerBlockData> MAP = new HashMap<>();




}
